# Formatting Hebrew Output (RTL)

- Always write Hebrew responses with Right-to-Left (RTL) alignment.
- Wrap content blocks in `<div dir="rtl">` or ensure inline elements, punctuation, and mixed English/Hebrew terms are strictly aligned RTL so reading is natural and unobstructed.
